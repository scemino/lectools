name: lectools
description: A tool to extract room images from LEC files.
version: 1.0.0
homepage: https://github.com/scemino/lectools

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  args: ^2.0.0
  path: ^1.8.0
  image: ^3.0.2

dev_dependencies:
  pedantic: ^1.9.0
